server {
    listen 5500;
    server_name localhost;

    root /var/www/html;
    index admin.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /api/ {
        proxy_pass http://identity-api:5299;
    }
}